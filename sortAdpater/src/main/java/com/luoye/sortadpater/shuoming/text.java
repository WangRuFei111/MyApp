package com.luoye.sortadpater.shuoming;
/**
 * created by: ls
 * TIME：2021/6/10
 * user：使用说名
 */
public class text {
    /**
     * LinearLayoutManager linearLayoutManager = new LinearLayoutManager(this);
     *         linearLayoutManager.setOrientation(LinearLayoutManager.HORIZONTAL);
     *         SortRecyclerAdapter sortRecyclerAdapter = new SortRecyclerAdapter(listVideoPath);
     *         recyclerView.setHasFixedSize(true);
     *         recyclerView.setAdapter(sortRecyclerAdapter);
     *         recyclerView.setLayoutManager(linearLayoutManager);
     *         new ItemTouchHelper(new SortCallback(sortRecyclerAdapter)).attachToRecyclerView(recyclerView);
     *         sortRecyclerAdapter.interfaceData(new OnData() {
     *             @Override
     *             public void sortOnClick() { 排序完成 }
     *             @Override
     *             public void addOnCliock() { 添加回调 }
     *         });
     */
}
